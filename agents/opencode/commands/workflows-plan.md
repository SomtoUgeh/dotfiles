---
description: "Transform feature descriptions into well-structured specs and executable PRDs"
agent: build
---

# Plan

Description: Transform feature descriptions into well-structured specs and executable PRDs

Arguments: `[feature description, bug report, or improvement idea]`

Preferred workflow: `workflows-plan`


<feature_description> $ARGUMENTS </feature_description>

Always load and execute the `workflows-plan` workflow/skill for this command.

Do not answer directly from this wrapper; the skill is the command body.
Pass the feature description above to the skill.
