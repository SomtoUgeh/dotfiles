# Zaraz reference maintenance

This directory contains the overview, Web API, configuration, patterns, and
troubleshooting references linked from [README.md](./README.md).

The previous implementation summary recorded stale line counts, a nonexistent
backup, and unsupported claims of verified APIs. It is not execution evidence.
Validate the current examples against the linked official API/settings pages
and test the chosen zone/tool before claiming an integration works.

The 2026-09-05 review corrected data-layer persistence, key/value setters, consent
method signatures/readiness/events, debug invocation, SPA configuration,
publication behavior, and Context Enricher handling. Local fixtures do not prove
live event delivery, consent behavior in every tool, or production compliance.
