# Machine-local overrides. Identity is folder-driven via includeIf —
# do not set user.email here (user.useConfigOnly is the safety net).
# Copy to ~/.gitconfig.local:
#   cp templates/gitconfig-local.template ~/.gitconfig.local
#
# The tracked git/.gitconfig includes this file via [include]. Git silently
# ignores it if absent.

[user]
	name = Somto Odera
